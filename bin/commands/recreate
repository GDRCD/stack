#!/usr/bin/env bash
# @cli.summary: Ricrea container e configurazione dello stack
# @cli.usage:

importLib "lib/lib-docker.sh"
shift
[[ $# -eq 0 ]] || { messageUnknownCommand recreate "$1"; exit 2; }
prompt -i "Ricreazione dello stack..."
dockerCompose up -d --force-recreate
